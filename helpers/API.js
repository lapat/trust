// API.js

var self =  module.exports = {
	newClap : function newClap (url) {
	    console.log("clap clicked for " + url);

	},
	newReport : function newReport (url) {
	    console.log("report clicked for " + url);
	}
}